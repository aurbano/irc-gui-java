package es.uniovi;

import java.awt.Color;
import java.awt.Graphics;
import java.awt.Rectangle;
import java.awt.SystemColor;

public class TabDesign extends javax.swing.plaf.basic.BasicTabbedPaneUI {

    /*@Override
    protected void paintTab(Graphics g, int tabPlacement, Rectangle[] rects, int tabIndex, Rectangle iconRect, Rectangle textRect) {
        Color savedColor = g.getColor();
        g.setColor(Color.WHITE);
        g.fillRect(rects[tabIndex].x, rects[tabIndex].y, rects[tabIndex].width, rects[tabIndex].height);
        g.setColor(Color.gray);
        g.drawRect(rects[tabIndex].x, rects[tabIndex].y, rects[tabIndex].width, rects[tabIndex].height);
        g.setColor(savedColor);
        bounds = getStringBounds(g, s);
        drawTextWithShadow(g, s, font, x + bounds.x + (w - bounds.width) / 2, y + bounds.y);
    }*/
 }
